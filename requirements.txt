# Core (required for the research loop, tasks, and tests)
numpy>=1.24
scikit-learn>=1.3
jsonschema>=4.0

# API layer (researchforge/api/server.py)
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0

# Optional: only needed to run tests/test_basic.py's API smoke-test-style
# checks locally with TestClient, and for the "npm-style" quick manual
# curl-free checks shown in the README.
httpx>=0.27

# Optional: only used by run_demo.py if you extend it to plot
# demo_results.json's curves yourself; the shipped demo prints a text report
# and writes JSON without requiring this.
matplotlib>=3.7
